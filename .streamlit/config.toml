# 画面はいつも明るい（ライト）背景で表示する。
# 色そのものはハードコードせず、Streamlit のテーマ機能で切り替えている。
# 端末の設定に合わせたい場合は base を "dark" にするか、この [theme] を丸ごと消す。
[theme]
base = "light"

# static/ の中身を <アプリのURL>/app/static/… で配信する。
# デプロイが反映されたかを外から確認するために version.txt を置いている。
[server]
enableStaticServing = true
